enum NavigationTabs {
  home,
  search,
  create,
  activity,
  profile,
}

enum HomeTabs {
  latest,
  trending,
  following
}

enum ProfileTabs {
  posts,
  savedPosts,
}

enum SearchResultsTabs {
  posts,
  profiles
}